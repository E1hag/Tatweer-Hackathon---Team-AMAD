import EmptyState from '../components/common/EmptyState';

export default function RequestsScreen() {
  return (
    <EmptyState
      title="Request board coming next"
      message="This screen will list local requests and let residents say they need this too."
    />
  );
}
